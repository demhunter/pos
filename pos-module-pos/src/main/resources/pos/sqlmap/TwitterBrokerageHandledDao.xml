<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pos.pos.dao.TwitterBrokerageHandledDao">
    <insert id="save" parameterType="com.pos.pos.dto.BrokerageHandledRecordDto">
        INSERT INTO brokerage_handled
        (
            user_id,
            amount,
            pay_mode,
            voucher,
            pay_time,
            remark
        ) VALUES (
            #{record.userId},
            #{record.amount},
            #{record.payMode},
            #{record.voucher},
            NOW(),
            #{record.remark}
        )
    </insert>

    <select id="queryHandledRecords" resultType="com.pos.pos.dto.BrokerageHandledRecordDto">
        SELECT
        <include refid="recordDtoColumns"/>
        FROM brokerage_handled bh
        WHERE bh.user_id = #{user.userId}
        LIMIT #{limitHelper.offset}, #{limitHelper.pageSize}
    </select>

    <sql id="recordDtoColumns">
        bh.`id` AS `id`,
        bh.user_id AS userId,
        bh.amount AS amount,
        bh.pay_mode AS payMode,
        bh.voucher AS voucher,
        bh.pay_time AS paytime,
        bh.remark AS remark
    </sql>
</mapper>
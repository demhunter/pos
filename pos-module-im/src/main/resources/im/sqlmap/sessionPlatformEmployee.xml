<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pos.im.dao.SessionPlatformEmployeeDao">

    <insert id="save">
        INSERT INTO im_session_platform_employee
        (session_id, pe_user_id, joined)
        VALUES
        (#{relation.sessionId}, #{relation.peUserId}, #{relation.joined})
    </insert>

    <select id="getBySessionId" resultType="com.pos.im.domain.SessionPlatformEmployee">
        SELECT
        `id` AS `id`,
        session_id AS sessionId,
        pe_user_id AS peUserId,
        joined AS joined
        FROM im_session_platform_employee
        WHERE session_id = #{sessionId}
    </select>
</mapper>